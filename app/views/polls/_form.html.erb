<%= simple_form_for(@poll) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.association :user %>
    <%= f.input :question %>
    <%= f.input :subtext %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
