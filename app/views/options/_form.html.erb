<%= simple_form_for(@option) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :poll %>
    <%= f.input :title %>
    <%= f.association :poll %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
