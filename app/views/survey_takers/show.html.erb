<div class="container">
        <div class="row">
            <div class="col-md-4 col-md-offset-4">
                <div class="login-panel panel panel-default">
                    <div class="panel-heading">
                        <h3 class="panel-title"><%= @survey_taker.number %></h3>
                    </div>                      
                    <div class="panel-body">
                        <p>
                        <% if @survey_taker.confirmed == 0 %>
                            Please confirm your account.
                        <% else %>
                            Your account has been activated. You will receive surveys to your phone via SMS now.
                        <% end %>
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>